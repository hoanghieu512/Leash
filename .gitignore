node_modules/
dist/
*.log

# Runtime state — never commit a real trading account's state or audit trail
state.json
audit.jsonl
fixtures/BLOCK_MATCH.txt

# Build artifact — chạy npm run build
bin/leash-hook.mjs
bin/leash-mcp.mjs
state.json.key
